{% extends 'NSIDocsBundle:Templates:template.html.twig' %}

{% block body %}
    <h1>Employes list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Poste</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for employe in employes %}
            <tr>
                <td><a href="{{ path('organisation_employe_show', { 'id': employe.id }) }}">{{ employe.id }}</a></td>
                <td>{{ employe.poste }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('organisation_employe_show', { 'id': employe.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('organisation_employe_edit', { 'id': employe.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('organisation_employe_new') }}">Create a new employe</a>
        </li>
    </ul>
{% endblock %}
