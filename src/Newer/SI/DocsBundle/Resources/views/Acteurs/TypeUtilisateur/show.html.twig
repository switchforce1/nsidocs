{% extends 'NSIDocsBundle:Templates:template.html.twig' %}

{% block main_content %}
    <h1>Typeutilisateur</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typeUtilisateur.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ typeUtilisateur.libelle }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ typeUtilisateur.description }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('acteurs_typeutilisateur_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('acteurs_typeutilisateur_edit', { 'id': typeUtilisateur.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
