{% extends 'AppBundle:Templates:template.html.twig' %}

{% block main_content %}
    <h1>Elements list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Numero</th>
                <th>Libelle</th>
                <th>Position</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for element in elements %}
            <tr>
                <td><a href="{{ path('document_element_show', { 'id': element.id }) }}">{{ element.id }}</a></td>
                <td>{{ element.numero }}</td>
                <td>{{ element.libelle }}</td>
                <td>{{ element.position }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('document_element_show', { 'id': element.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('document_element_edit', { 'id': element.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('document_element_new') }}">Create a new element</a>
        </li>
    </ul>
{% endblock %}
